const titleLabel = {
    DNA: 'hud/Label',
    rx:  .5,
    ry:  .42,
    font:  env.style.font.title.head,
    color: env.style.color.title,
    text:  () => {
        return res.txt.label.title
    }
}

