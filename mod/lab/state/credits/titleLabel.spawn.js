const titleLabel = {
    DNA: 'hud/Label',
    rx:  .5,
    ry:  .21,
    font:  env.style.font.title.head,
    color: env.style.color.title,
    text:  () => {
        return res.txt.label.credits
    }
}

