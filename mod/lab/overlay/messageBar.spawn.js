const messageBar = {
    DNA: 'hud/ArcadeText',

    rx:  .5,
    ry:  .25,
    align: 'center',

    font:    env.style.font.messageBar.head,
    subFont: env.style.font.subMessageBar.head,
    step:    env.style.font.messageBar.size + 4,

    color:   env.style.color.messageBar.front,
    // outline: env.style.color.messageBar.outline,
    shadowColor: env.style.color.messageBar.outline,
}
